import Link from "next/link"; import { notFound } from "next/navigation"; import { Page } from "../../components"; import { posts } from "../../content";
export function generateStaticParams(){return posts.map(({slug})=>({slug}));}
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const post=posts.find(p=>p.slug===slug); if(!post) notFound(); return <Page><article className="article"><header className="article-head shell narrow"><Link href="/blog" className="back">← All notes</Link><p className="eyebrow">{post.category}</p><h1>{post.title}</h1><p className="lede">{post.excerpt}</p><small>{post.date} · {post.read}</small></header><div className="article-body"><p className="intro">The best small tools rarely announce their complexity. They meet a specific need, fit naturally into a person’s day and then quietly get out of the way.</p><h2>Begin with the useful part</h2><p>It is tempting to start a new project by listing everything it could become. A better place to begin is the smallest meaningful change it can make for someone. That gives the work a centre of gravity.</p><p>Constraints are helpful here. They make priorities visible, sharpen the language around the idea and leave more time to care for the moments that people actually experience.</p><blockquote>Good editing is a form of generosity: less to learn, less to manage and more room for the thing that matters.</blockquote><h2>Clarity is part of the craft</h2><p>A clear product does not need to be plain or impersonal. Character can live in rhythm, type, colour and small moments of delight. The test is whether those choices support understanding rather than compete with it.</p><p>That is the work Whimbr keeps returning to: making useful things, finding the right level of detail and learning in public along the way.</p></div></article></Page>}
