import Link from "next/link"; import { notFound } from "next/navigation"; import { Page } from "../../components"; import { posts } from "../../content"; import { getPublishedBySlug } from "../../content-db";
export function generateStaticParams(){return posts.map(({slug})=>({slug}));}
export const dynamic="force-dynamic";
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const managed=await getPublishedBySlug("post",slug);const base=posts.find(p=>p.slug===slug);if(!managed&&!base)notFound();const post=managed?{...managed,excerpt:managed.summary,date:managed.publishedAt?.toLocaleDateString("en-AU")||"Recently",read:"5 min read"}:base!;return <Page><article className="article"><header className="article-head shell narrow"><Link href="/blog" className="back">Blog › {post.category}</Link><p className="eyebrow">{post.category}</p><h1>{post.title}</h1><p className="lede">{post.excerpt}</p><div className="author"><span className="logo-mark">W</span><b>Whimbr Studio</b><small>{post.date} · {post.read}</small></div></header><div className="article-cover"/><div className="article-body">{managed?<>{managed.body.split(/\n\n+/).map((x,i)=><p className={i===0?"intro":""} key={i}>{x}</p>)}</>:<><p className="intro">The best small tools rarely announce their complexity. They meet a specific need, fit naturally into a person’s day and then quietly get out of the way.</p><h2>Begin with the useful part</h2><p>It is tempting to start a new project by listing everything it could become. A better place to begin is the smallest meaningful change it can make for someone.</p><blockquote>Good editing is a form of generosity: less to learn, less to manage and more room for the thing that matters.</blockquote><h2>Clarity is part of the craft</h2><p>A clear product does not need to be plain or impersonal. Character can live in rhythm, type, colour and small moments of delight.</p></>}</div></article></Page>}
