import Link from "next/link"; import { Arrow, Page } from "../components"; import { posts } from "../content";
export const metadata={title:"Blog | Whimbr Studio",description:"Notes on designing, building and learning from Whimbr Studio."};
export default function Blog(){return <Page><section className="page-hero shell"><p className="eyebrow">The notebook</p><h1>Notes on <em>making.</em></h1><p className="lede">Working thoughts on thoughtful software, useful objects, visual stories and the process of bringing small ideas to life.</p></section><section className="section shell"><div className="feature-post"><div><p className="eyebrow">Featured · Making</p><h2>{posts[0].title}</h2><p>{posts[0].excerpt}</p><Link href={`/blog/${posts[0].slug}`} className="button">Read the article <Arrow/></Link></div><div className="feature-art" aria-hidden="true"><span/><span/><span/></div></div><div className="post-list blog-list">{posts.slice(1).map(post=><Link href={`/blog/${post.slug}`} key={post.slug}><span>{post.category}</span><h2>{post.title}</h2><p>{post.excerpt}</p><small>{post.date} · {post.read}</small></Link>)}</div></section></Page>}
