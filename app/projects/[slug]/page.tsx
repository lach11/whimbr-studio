import Link from "next/link"; import { notFound } from "next/navigation"; import { Arrow, Page } from "../../components"; import { projects } from "../../content";
export function generateStaticParams(){ return projects.map(({slug})=>({slug})); }
export default async function ProjectDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const project=projects.find(p=>p.slug===slug); if(!project) notFound(); return <Page><article><header className={`detail-hero ${project.color}`}><div className="shell"><Link href="/projects" className="back">← All projects</Link><p className="eyebrow">{project.category}</p><h1>{project.title}</h1><p className="lede">{project.summary}</p><div className="meta"><span><b>Status</b>{project.status}</span><span><b>Year</b>{project.year}</span></div></div></header><div className="section shell case-study"><section><p className="eyebrow">The challenge</p><h2>{project.challenge}</h2></section><section><p className="eyebrow">The approach</p><p className="big-copy">{project.approach}</p><h3>What it includes</h3><ul>{project.outcomes.map(x=><li key={x}>{x}</li>)}</ul></section></div><section className="shell callout"><div><p className="eyebrow">Have a question?</p><h2>Get help or share an idea.</h2></div><Link href="/support" className="button cream">Visit support <Arrow/></Link></section></article></Page>}
