import Link from "next/link"; import { Arrow, Page } from "../components"; import { projects } from "../content";
export const metadata = { title: "Projects | Whimbr Studio", description: "Software, electronics, data visualisations and learning projects by Whimbr Studio." };
export default function Projects(){ return <Page><section className="page-hero shell"><p className="eyebrow">Projects</p><h1>Ideas made <em>tangible.</em></h1><p className="lede">A growing catalogue of tools, experiments and teaching projects—each shaped by a real question or need.</p></section><section className="section shell"><div className="filter-row" aria-label="Project categories"><span className="active">All projects</span><span>Apps</span><span>Electronics</span><span>Data & maps</span><span>Courses</span></div><div className="project-grid projects-all">{projects.map(p=><Link href={`/projects/${p.slug}`} className={`project-card ${p.color}`} key={p.slug}><div className="project-visual"><span className="visual-label">{p.status}</span><div className="visual-shape"/></div><div className="card-copy"><p>{p.category}</p><h2>{p.title}</h2><span>{p.summary}</span><b>View case study <Arrow/></b></div></Link>)}</div></section></Page> }
