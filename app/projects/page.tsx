import Link from "next/link";import {Page,PageTitle} from "../components";import {getPublished} from "../content-db";
export const metadata={title:'Projects | Whimbr Studio',description:'Software, electronics, data visualisations and learning projects by Whimbr Studio.'};export const dynamic='force-dynamic';
export default async function Projects(){const all=await getPublished('project');return <Page><div className="cr-container cr-page"><PageTitle>Projects</PageTitle><p className="cr-intro">Apps, experiments, tools and creations. Some are small, some are ambitious, all are built with curiosity.</p><div className="cr-filter"><span className="active">All</span><span>Apps</span><span>Electronics</span><span>Data</span><span>Courses</span></div><div className="cr-portfolio-two">{all.map(p=><Link href={`/projects/${p.slug}`} className="cr-project-card" key={p.slug}>{p.imageUrl?<img src={p.imageUrl} alt=""/>:<div className="cr-image-placeholder">W</div>}<div><h2>{p.title}</h2><p>{p.summary}</p><small>{p.category}</small></div></Link>)}</div></div></Page>}
