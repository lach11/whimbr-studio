import {Page,PageTitle} from "../components";
import {getPublished} from "../content-db";
const icons=["/course-discover.png","/course-plan.png","/course-learn.png","/course-apply.png"];
export const metadata={title:"Learning | Whimbr Studio",description:"Practical Whimbr Studio courses for learning by making."};
export default async function Learning(){const courses=await getPublished("course");return <Page><div className="cr-container cr-page"><PageTitle>Learning</PageTitle><p className="cr-intro">Practical courses designed to take you from a useful idea to something you can confidently build and use.</p><div className="cr-section-title"><h2>Courses</h2><i/></div><div className="cr-learning-grid">{courses.map((course,index)=><article className="cr-learning-item" key={course.slug}><span><img src={icons[index%icons.length]} alt=""/></span><div><small>{course.category}</small><h2>{course.title}</h2><p>{course.summary}</p>{course.linkUrl?<a className="cr-button" href={course.linkUrl} target="_blank" rel="noreferrer">View course on Udemy →</a>:<em>Udemy link coming soon</em>}</div></article>)}</div>{courses.length===0&&<p className="cr-empty">Courses can now be added and published from the CMS.</p>}</div></Page>}
